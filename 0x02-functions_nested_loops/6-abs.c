#include "main.h"
#include <stdlib.h>

/**
 * _abs - entry point
 * @int: integer to be computed
 * Desc: Ouput's the absolute value of an integer
 * Return: Always 0
 */

int _abs(int)
{
	if (int >= 0)
	{
		return (int);
	}
	else if (int < 0)
	{
		return (-int);
	}
}
